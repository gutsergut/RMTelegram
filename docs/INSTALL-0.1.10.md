# ⚠️ КРИТИЧЕСКОЕ ОБНОВЛЕНИЕ: pkg_radicalmart_telegram-0.1.10

## Что случилось?

После установки предыдущей версии пакета 0.1.10 всё равно загружалось **0 ПВЗ** вместо ожидаемых ~50 000.

## Причина

**Версии в XML манифестах не были обновлены!**

Joomla при установке проверяла версию расширения:
- В манифесте было: `<version>0.1.0</version>`
- В базе уже установлена: `0.1.0`
- **Вывод:** версия не изменилась → файлы НЕ перезаписываются → исправленный код НЕ применяется!

## Что исправлено в новой сборке

✅ Обновлены версии во ВСЕХ XML манифестах до `0.1.10`:
- Компонент `com_radicalmart_telegram`
- Плагин `system/radicalmart_telegram`
- Плагин `task/radicalmart_telegram_fetch`
- Плагин `radicalmart_payment/telegramcards`
- Плагин `radicalmart_payment/telegramstars`
- Пакет `pkg_radicalmart_telegram`

✅ Сохранено критическое исправление: `$operation = [2, 3];` вместо `['giveout']`

## Инструкция по установке

### Вариант 1: Удалить старое + Установить новое (РЕКОМЕНДУЕТСЯ)

```
1. Система → Расширения → Управление
2. Найдите "RadicalMart Telegram" или "pkg_radicalmart_telegram"
3. Удалите пакет (это удалит компонент и все плагины)
4. Система → Расширения → Установить
5. Выберите файл: dist\pkg_radicalmart_telegram-0.1.10.zip
6. Установите
7. Настройте заново параметры (API токен, провайдеры)
8. Запустите обновление ПВЗ
```

### Вариант 2: Принудительное обновление через FTP/SSH (для продвинутых)

```
1. Скачайте пакет pkg_radicalmart_telegram-0.1.10.zip
2. Распакуйте локально
3. Через FTP/SSH замените файлы:
   - administrator/components/com_radicalmart_telegram/*
   - plugins/system/radicalmart_telegram/*
   - plugins/task/radicalmart_telegram_fetch/*
   - plugins/radicalmart_payment/telegramcards/*
   - plugins/radicalmart_payment/telegramstars/*
4. В базе данных обновите версии в таблице tw9cs_extensions:
   UPDATE tw9cs_extensions
   SET manifest_cache = JSON_SET(manifest_cache, '$.version', '0.1.10')
   WHERE element IN ('com_radicalmart_telegram', 'radicalmart_telegram',
                     'radicalmart_telegram_fetch', 'telegramcards', 'telegramstars')
5. Очистите кеш Joomla
```

## Проверка

После установки:

1. **Проверьте версию:**
   ```
   Компоненты → RadicalMart Telegram → Status
   Внизу страницы должно быть: "Version 0.1.10"
   ```

2. **Запустите обновление ПВЗ:**
   ```
   Компоненты → RadicalMart Telegram → Status → кнопка "Обновить базу ПВЗ"
   ```

3. **Проверьте логи:**
   ```
   administrator/logs/com_radicalmart_telegram.php

   Должны быть записи:
   2025-11-05 ... INFO ApiShip fetch started: providers=yataxi,cdek,x5
   2025-11-05 ... INFO ApiShip provider 'yataxi': total points = 17576
   2025-11-05 ... INFO ApiShip provider 'cdek': total points = 7532
   2025-11-05 ... INFO ApiShip provider 'x5': total points = 24791
   2025-11-05 ... INFO ApiShip fetch completed: total=49899
   ```

4. **Ожидаемый результат:**
   ```
   ✅ Загружено ~50 000 точек ПВЗ (yataxi: 17K, cdek: 7.5K, x5: 24K)
   ```

## Файл пакета

📦 `dist\pkg_radicalmart_telegram-0.1.10.zip` (0.08 MB)

---

**Дата:** 5 ноября 2025, финальная сборка
**Важность:** 🔴 КРИТИЧЕСКАЯ
